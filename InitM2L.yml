YAMLCopiar 

      trigger:  
      
      - main 
      
      pool:  
         vmImage: ubuntu-latest 
      
      jobs: 
      - deployment: VMDeploy 
        displayName: Deploy to VM 
        environment:  
         name: VMenv 
         resourceType: virtualMachine 
         resourceName: vm14m2 # only deploy to the VM resource named vm14m2 
      
        strategy: 
      
          runOnce: 
      
            deploy:    
      
                steps: 
      
                - script: echo "Hello world"
