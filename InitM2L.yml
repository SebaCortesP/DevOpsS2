trigger:
  - main 

pool:
  vmImage: ubuntu-latest

jobs:
- deployment: VMDeploy
  displayName: Deploy to VM
  environment:
    name: Grupo_14
    resourceType: virtualMachine
    resourceName: vm14m2  # debe coincidir exactamente con el nombre del recurso VM registrado
  strategy:
    runOnce:
      deploy:
        steps:
          - script: echo "Hello world"
            displayName: "Test script"

